{
  "name": "homevisit",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mock": "mockoon-cli start --data ./mock/mockoon-env.json",
    "mock:status": "mockoon-cli start --data ./mock/mockoon-env.json --port 5002"
  },
  "devDependencies": {
    "@mockoon/cli": "^9.4.0"
  }
}
