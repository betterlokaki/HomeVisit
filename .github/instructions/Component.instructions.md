---
applyTo: "**/*.ts"
---

When writing any new frontend feature, first you make componenet for each thing, with drilling properties with best practices out there

Or using the context, which is also divided logic by the best practices for frontend

You never never never use px you always use % and or relative size,
We never know which screen will see it
